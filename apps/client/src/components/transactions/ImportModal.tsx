interface ImportModalProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}

export function ImportModal(_props: ImportModalProps) {
  // TODO: import modal
  return null;
}

export default ImportModal;
