// Public API for the dashboard feature — import from here, not from internal files.
// Note: dashboard composes other features (transactions, budgets) via their
// front doors, e.g. `import { ... } from '@/features/transactions'`.
export {};
