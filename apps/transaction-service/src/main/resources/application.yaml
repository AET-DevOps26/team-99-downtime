spring:
  application:
    name: transaction-service
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: ${AUTH_JWKS_URI:http://auth-service:3000/api/auth/jwks}

server:
  port: 8080

service:
  me-path: /api/transactions/me

auth:
  issuer: ${AUTH_ISSUER:http://localhost:9099}

services:
  budget:
    url: ${BUDGET_SERVICE_URL:http://budget-service:8082}
