ExpenseFlow deployed to namespace {{ .Release.Namespace }}.

App URL: https://{{ .Values.domain }}
{{- if .Values.drizzleStudio.enabled }}

Drizzle Studio
  URL:      https://studio.{{ .Values.domain }}
  Auth:     GitHub OAuth (org: {{ if .Values.drizzleStudio.github.org }}{{ .Values.drizzleStudio.github.org }}{{ else }}any GitHub user{{ end }})
  Databases: auth_db, transaction_db, budget_db (pre-configured)
{{- end }}
