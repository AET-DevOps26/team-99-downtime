apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: t99-stage
  labels:
    app.kubernetes.io/name: grafana
    app.kubernetes.io/part-of: t99-observability
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: grafana
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
